import editCode from './editCode';
import executeCode from './executeCode';
import handleCodeEdited from './handleCodeEdited';
import handleExecuteCodeClicked from './handleExecuteCodeClicked';
import resetState from './resetState';
export default {
  editCode: editCode,
  executeCode: executeCode,
  handleCodeEdited: handleCodeEdited,
  handleExecuteCodeClicked: handleExecuteCodeClicked,
  resetState: resetState
};
export { editCode, executeCode, handleCodeEdited, handleExecuteCodeClicked, resetState };